<div>
<h1 class= "w3-animate-opacity">All Orchestras</h1>
</div>
<ul class="w3-ul w3-hoverable">
    <% @orchestras&.each do |orchestra| %>
        <li><%= link_to orchestra.name, orchestra_path(orchestra) %> | Conductor: <%= orchestra.conductor %></li>
    <% end %>
</ul>